% find the standard deviation of the fit
function f=dev(fit,data)
    a = size(exp);
    a = a(2);
    b = a(1);
    e = zeros(1,a);
        for j=1:b
            e(j) = e(j) + (exp(1,j) - vals(1,j))^2;
        end
    f = 1.96*sqrt(e/(length(exp)-2));
end